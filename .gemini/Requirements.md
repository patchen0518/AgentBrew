# AgentBrew Requirements
- Must act as a universal MCP server.
- Must install packages from Git URLs.
- Must support Polyglot packages (MCPs, CLI, Markdown skills).
- Must manage child process lifecycles.
